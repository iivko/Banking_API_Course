services:
  postgres:
    build:
      context: .
      dockerfile: backend/docker/local/postgres/Dockerfile
    ports:
      - "5432:5432"
    volumes:
      - iivko_bank_local_db:/var/lib/postgresql/data
    env_file:
      - ./.envs/.env.local
    networks:
      - iivko_bank_local_nw

networks:
  iivko_bank_local_nw:
    external: true

volumes:
  iivko_bank_local_db:
